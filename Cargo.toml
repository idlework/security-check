[package]
name = "security-check"
version = "0.1.0"
edition = "2021"
description = "macOS security audit CLI tool"

[dependencies]
clap = { version = "4", features = ["derive"] }
colored = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
